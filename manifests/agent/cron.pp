
class puppet::agent::cron
{
  #$ppt_cmd = hiera('puppet_cmd')
  #cron { "puppet":
  #  ensure  => present,
  #  command => $ppt_cmd,
  #  user    => 'root',
  #  hour    => fqdn_rand(6),
  #  minute  => fqdn_rand(60)
  #}
}
